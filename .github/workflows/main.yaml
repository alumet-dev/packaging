---
# This file is used to test the packaging workflows.
# This "Main pipeline" can be triggered manually.
name: Main pipeline
on:
  workflow_dispatch:
    inputs:
      version:
        description: Application version
        type: string
        required: false
        default: '0.0.0'
      revision:
        description: Package revision/release number
        required: false
        type: number
        default: 1
      tag:
        description: Tag to checkout in alumet repo
        type: string
        required: false
        default: 'main'

      packaging-repository:
        description: "The github 'organization/repository' to use for packaging"
        required: false
        type: string
        default: "alumet-dev/packaging"

      alumet-repository:
        description: "The github 'organization/repository' to use for alumet"
        required: false
        type: string
        default: "alumet-dev/alumet"
jobs:
  rpm:
    uses: ./.github/workflows/build_rpm.yaml
    strategy:
      fail-fast: false
      matrix: 
        arch: [x86_64, aarch64]
    with:
      arch: ${{ matrix.arch }}
      version: ${{ inputs.version }}
      release-version: ${{ inputs.revision }}
      tag: ${{ inputs.tag }}
      alumet-repository: ${{ inputs.alumet-repository }}
      packaging-repository: ${{ inputs.packaging-repository }}

  deb:
    uses: ./.github/workflows/build_deb.yaml
    strategy:
      fail-fast: false
      matrix: 
        arch: [x86_64, aarch64]
    with:
      arch: ${{ matrix.arch }}
      version: ${{ github.event.inputs.version }}
      revision: ${{ github.event.inputs.revision }}
      tag: ${{ inputs.tag }}
      alumet-repository: ${{ inputs.alumet-repository }}
      packaging-repository: ${{ inputs.packaging-repository }}

  docker:
    uses: ./.github/workflows/build_docker.yaml
    strategy:
      fail-fast: false
      matrix: 
        arch: [x86_64, aarch64]
    with:
      arch: ${{ matrix.arch }}
      version: ${{ inputs.version }}
      release-version: ${{ inputs.revision }}
      packaging-repository: ${{ inputs.packaging-repository }}
    needs:
      - rpm
      - deb
